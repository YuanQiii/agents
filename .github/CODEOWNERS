# CODEOWNERS — 约束层面与门禁的归属
#
# 生效中：`main` 已开启分支保护并勾选「Require review from Code Owners」（2026-09-20 设置）。
#
# ⚠️ 但 `enforce_admins = false` —— **管理员（@YuanQiii）本人可以直接推 `main`，不会被这里拦下**。
#    本文件拦的是协作者与自动化：它们改下面这些文件必须经 owner 审批。
#    所以它**不替代** Gate（`pnpm typecheck && pnpm lint && pnpm fmt`），只保证「改约束层」会经过一次人工过目。
#
# 为什么这些文件值得单独指定 owner：它们会**静默**影响之后每一个 agent 与协作者的行为，
# 而且改错了不会有任何报错。这正是 docs/anti-rot-plan.md 里说的「缺失型腐烂」的入口。

# 约束文档本身
/AGENTS.md                                @YuanQiii
/packages/AGENTS.md                       @YuanQiii
/CONTEXT.md                               @YuanQiii
/docs/                                    @YuanQiii

# 门禁与自检配置：改这些等于改了「什么算合格」
/.oxlintrc.json                           @YuanQiii
/.oxfmtrc.json                            @YuanQiii
/eslint.config.js                         @YuanQiii
/tsconfig.json                            @YuanQiii
/scripts/check-agents-md.mjs              @YuanQiii
/.github/                                 @YuanQiii
