# 2026-09-20 工作日志

## 收尾：约束体系首次入库

上一轮遗留三件事，本轮全部处理完。

**1. 依赖已修（不是我修的——`node_modules` 已被修好）**：`typecheck` 与 `eslint` **同时转 PASS**，
证实了此前的归因判断——那 13 个 typecheck 错误与 90 个 eslint 错误**全部同源**于 axios / @unocss /
`@typescript-eslint/parser` 的空壳依赖，不是代码问题。`Cannot find module` 后面跟着的一串类型错误都是派生的。

**2. 防腐第 ④ 层（定期复核）已落地**：`linter.yml` 加 `schedule`（每周一 09:00 UTC）+ `workflow_dispatch`；
并给 super-linter job 加 `if: github.event_name != 'schedule'`，让每周那次只跑零依赖的约束文档检查。
放在已有 workflow 里而不是新建一个，是为了不重复那条命令。

**3. 约束体系已入库**：5 个提交，全部通过 pre-commit 的完整 Gate 与 commit-msg 校验。

```
51ffad0 chore(ci): 加约束文档自检脚本与 PR 级漂移检测
0242ad5 chore(memory): 工作区记忆更新、瘦身与防腐记录
16f4494 docs(adr): 术语表、3 条决策记录与防腐层方案
cb6946f docs(agents): 建立约束体系（根路由器 + 6 篇场景文档 + 目录级）
9cb28a3 chore(gate): 钉 pnpm 11 并补齐依赖声明，给约束文档加门禁与定期复核
```

### 本轮踩的两个坑（都值得记住）

**坑 A：git 钩子里的裸 `pnpm` 在 Git-Bash 下会崩。** pre-commit 内容是 `pnpm typecheck && ...`，
而本机 `pnpm`（无扩展名的 shim）在 MSYS 下会拼出被改写的路径而抛错 → 钩子第一步即失败。
`pnpm.cmd` 与 corepack 入口都正常，但钩子用的是裸 `pnpm`，改不了。
**修法（不动仓库、不用 `--no-verify`）**：造一个垫片目录前置到 PATH，再提交——
```sh
printf '#!/bin/sh\nexec node "C:/nvm4w/nodejs/node_modules/corepack/dist/pnpm.js" "$@"\n' > $SHIM/pnpm
chmod +x $SHIM/pnpm
PATH="$SHIM:$PATH" git commit ...
```
这是**让环境能按原意跑钩子**，不是绕过它。

**坑 B：带 pathspec 的 `git commit -- <paths>` 会重置索引里其他文件。**
本仓库 pre-commit 末步是 `git diff --exit-code`（要求工作区与索引一致），于是分主题提交时：
先 `git add -A` 再 `git commit -- 文件子集` → git 把索引里**其余**文件重置回 HEAD → 钩子判定工作区不干净 → 提交被拒
（且 HEAD 不动，容易误以为提交成功）。
**修法**：先 `git reset`，然后「**按组暂存 + 不带 pathspec**」逐个提交——
只要**已跟踪且被修改**的文件全部进第一个提交，其余都是未跟踪文件（对 `git diff` 不可见），后续提交就不会再触发这个矛盾。

### 最终验证（提交后实测）

```
- fmt:          PASS（oxfmt --check，297 文件）
- lint:         oxlint 0 warning / 0 error（235 文件 / 141 规则）；eslint PASS
- typecheck:    PASS
- agents-check: PASS（链接可达 · 命令真实 · 无章节号指针）
- test:         NOT AVAILABLE（ADR-0002）
- 4 个 workflow YAML 均可解析；linter.yml 的 on = pull_request + schedule + workflow_dispatch
```

### 仍未做

- `.github/CODEOWNERS`：缺 owner 名（信息缺口，不是技术障碍）。
- 防腐第 ⑤ 层（代理级验证 / proof loop）：理由见 `docs/anti-rot-plan.md`。
- 依赖声明欠账：`@sa/uno-preset` 缺 `@unocss/core` + `@unocss/preset-mini`；根缺 `axios`（属依赖变更，须先问）。

## 收尾（续）：CODEOWNERS、推送到远程、CI 实测通过

用户给出 `YuanQiii` 与 `git@github.com:YuanQiii/agents.git`，补齐我上一轮列出的两个阻塞项。

**1. `.github/CODEOWNERS` 已建**（`@YuanQiii` 拥有约束文档、门禁配置与自检脚本）。
文件里写明：**它只声明归属，实际拦截需要仓库开启 branch protection 并勾选 Require review from Code Owners**——
否则它不会拦下任何东西（属于「写了但没生效」，容易误以为有保护）。

**2. 已推送到 `YuanQiii/agents`（私有，默认分支 main）**，HEAD `82ce467`。
remote 配置是**混合**的：`fetch` 用用户给的 SSH 地址、`push` 指向 HTTPS。
原因：本机**没有 SSH key**（`~/.ssh` 不存在、agent 无 key）→ SSH 一律 `Permission denied (publickey)`；
但 **Git Credential Manager 里存有 GitHub 凭据**（`username=YuanQiii`，`gho_` OAuth token），HTTPS 可用。
装好 SSH key 后一条命令即可把 push 换回 SSH。

**3. 环境级发现（写进了用户级记忆）**：env 里的代理端口 **4780 是坏的**（HTTPS 走它报 `CONNECT tunnel failed, response 502`），
**可用的是 7897**；直连 GitHub 也失败（`schannel: server closed abruptly`）。
所以访问 GitHub 必须显式 `-c http.proxy=http://127.0.0.1:7897` / `curl -x http://127.0.0.1:7897`，不能依赖环境变量。
另：本机**没有 `gh` CLI**（`gh: command not found`），所以一切 GitHub 操作走 REST API + 上面的凭据。

**4. CI 侧验收已完成（这是上一轮唯一没闭环的事）**：用 API 触发 `workflow_dispatch`，
run `35479351471` → **success**；两个 job 全绿：
- `Lint All Code`（super-linter，5 步全 success）
- `Check Constraint Docs`（我们的 `node scripts/check-agents-md.mjs`）——**约束文档门禁在 GitHub 上真的跑通了**

**仍未验证**：`agents-md-drift.yml` 只由 `pull_request` 触发，本地 12 条真值用例测过判定逻辑，
但**「`gh` 在 runner 上留言并自动删除评论」这一步从未在真 PR 上跑过**。要真正确认，需要开一个（可随时关掉的）测试 PR。

**顺带一个结构事实**：`linter.yml` 没有 `push` 触发器 → **推送到 main 本身不跑任何 CI**（只有 PR、每周一、手动触发起作用）。
这是上游模板的形态，我没擅自改；如果要让 main 每次推送都过门禁，加 `push: branches: [main]` 即可。
